[project]
name = "glean-enterprise-search-claim-fence"
version = "0.1.0"
description = "Deterministic entitlement-aware enterprise-search evidence verifier"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
